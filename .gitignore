node_modules/
.env
.env.*
!.env.example
.idea/
.DS_Store
dist/
coverage/
*.log

# Claude Code tooling — local config and installed skills, not application code
.claude/
.agents/
skills-lock.json

# Redis persistence dump (created at runtime, never source)
dump.rdb
